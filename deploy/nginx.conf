server {
    listen 80;
    server_name gsmart.strangled.net gsmarttool.com www.gsmarttool.com;

    # Redirect HTTP to HTTPS (uncomment after setting up SSL)
    # return 301 https://$host$request_uri;

    client_max_body_size 100M;

    location /static/ {
        alias /home/deploy/audit/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /home/deploy/audit/media/;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# HTTPS server block (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name gsmarttool.com www.gsmarttool.com;
#
#     ssl_certificate /etc/letsencrypt/live/gsmarttool.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/gsmarttool.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 100M;
#
#     location /static/ {
#         alias /home/deploy/audit/static/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /home/deploy/audit/media/;
#         expires 7d;
#     }
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 75s;
#     }
# }
